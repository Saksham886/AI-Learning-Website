services:
  - type: web
    name: flask-backend
    env: python
    buildCommand: |
      pip install -r requirements.txt
      npx playwright install chromium --with-deps
    startCommand: python app.py
